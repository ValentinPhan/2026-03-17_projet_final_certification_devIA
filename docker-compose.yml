services:
  api_data:
    build: .
    env_file: .env
    ports:
      - "${API_DATA_PORT}:8001"

  api_ia:
    build: .
    env_file: .env
    ports:
      - "${API_IA_PORT}:8000"

  streamlit:
    build: .
    env_file: .env
    ports:
      - "${STREAMLIT_PORT}:8501"
